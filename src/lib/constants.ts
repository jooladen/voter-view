/** 고정 헤더 높이 (px). globals.css의 --header-height와 동기화 */
export const HEADER_HEIGHT = 72;

/** 스크롤 트리거 임계값 (px). 이 값 이상 스크롤 시 헤더 배경 활성화 */
export const SCROLL_THRESHOLD = 50;

/** 공통 애니메이션 ease 커브 */
export const EASE_CURVE: [number, number, number, number] = [0.25, 0.46, 0.45, 0.94];

/** ScrollReveal/TextReveal useInView margin */
export const REVEAL_MARGIN = "-80px";
